# Setup: see CONTRIBUTING.md.
pre-commit:
  commands:
    gofmt:
      glob: "*.go"
      run: make check-format FILES="{staged_files}"

pre-push:
  commands:
    lint:
      glob: "*.go"
      run: make lint
